#include<stdio.h>
#include<math.h>
int main()
{

    int a = 2;

    printf("sqrt of a is : %f",sqrt(a));
    printf("\n sqrt of 4 is : %f",sqrt(4));
    float b=2.99;
    printf("\nCeil value of b is : %f",ceil(b));
    printf("\nFloor value of b is : %f",floor(b));
    return 0;
}
